
# Raw byte-layout fallbacks (NEED VERIFY WITH OFFICIAL SDK/DOCS)
raydium_clmm:
  # Offsets inside CLMM pool account (bytes)
  sqrt_price_x64: { off: 8, size: 16 }
  liquidity:      { off: 24, size: 16 }
  fee_tier_bps:   { off: 40, size: 2  }
  tick_spacing:   { off: 0,  size: 4  }

meteora_dlmm:
  head:
    base_fee_bps: { off: 4, size: 2 }
    bin_count:    { off: 0, size: 4 }
  bin:
    size: 32
    price_x64: { off: 0,  size: 16 }
    liq:       { off: 16, size: 16 }
